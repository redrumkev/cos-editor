## Summary

<!-- What does this PR do? -->

## Test Plan

- [ ] `make ci-fast` passes (biome + typecheck + unit tests)
- [ ] `make build` succeeds
- [ ] Manual smoke test: `make dev` — app launches, editor visible

## Checklist

- [ ] No `npm`, `eslint`, or `prettier` usage
- [ ] IPC channels defined in `src/shared/ipc.ts`
- [ ] Types match COS API schemas
